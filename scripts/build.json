{
    "tasks": [
        {
            "label": "compile-java",
            "command": "javac",
            "type": "shell",
            "args": [
                "-sourcepath",
                "src",
                "-d",
                "build",
                "${file}"
            ]
        }
    ]
}